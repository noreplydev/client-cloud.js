export const config = {
    PORT : 3000, // listening port of the server
    PROTOCOL : 'http', // http or https
    HOSTNAME : 'localhost', // ip or localhost
    DIRECTORY_DELIMITER : '+' // defined delimiter on the server 
} 